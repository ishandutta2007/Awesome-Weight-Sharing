# Temporal Weight Sharing (Recurrent Networks)

Uses an identical, fixed transition weight matrix across every chronological time step.

## Architecture Diagram
`mermaid
flowchart TD
    A[Input] --> B[Temporal Weight Sharing (Recurrent Networks)]
    B --> C[Output]
`

[Back to README](../README.md)
